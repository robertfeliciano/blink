class Point { 
    let x: i8 = 0;
    let y: bool = false;
}

fun main(argv: [string; 2]) => i32 {
    let p = new Point { x = 2, y = true};
    let x = p.x + (9 as i8);
    return (x + 14 as i8) as i32;
    //let y = p.y;
}