class Point { 
    let x: i32;
    let y: i32;

    fun sumX(other: Point) => i32 {
        return this.x + other.x;
    }

    fun midpt(other: Point) => Point {
        return new Point {
            x = (this.x + other.x)/2,
            y = (this.y + other.y)/2
        };
    }
}

fun panic() => void {
    return;
}

fun fib(n: i32) => i32 {
    if n <= 1 {
        return n;
    } else {
        return fib(n-1) + fib(n-2);
    }
}

fun main(argc: i32) => i32 {
    /*
    let super_nested = [[[1,2],[3,4]],[[5,6],[7,8]]];
    let p = new Point { x = 22, y = 9 };
    let p_0 = new Point { x = 10, y = 9 };
    return p.midpt(p_0).x + super_nested[0][1][1];
    

    let x: i32;
    let a = 12;
    let b = 99;
    if a < b {
        x = 4;
    } else { 
        x = 3;
    }
    return x;
    */
/*
    while not true {
        let a = 0;
    }
    

    let s = "hello world\n";
    printf(s);
    let g = -4.5;
    //return 2e2 as i32;
    */
    let x = 1;
    x <<= 1;
    if false {
        x <<= 3;
    } else {
        x >>>= 2;
    }
    //return x;
    return 5 + (-3);
    //return fib(8);
    //return -2.5 as i32;
    //return (g + 4.5) as i32;
/*
    let x = 10;
    for i in 10..20 {
        if i == 12 {
            break;
        }
        x = x + -1;
    }
    return x;
    */
}