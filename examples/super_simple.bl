fun main(argv: [string; 2]) => u64 {
    let x: u8 = otherFunc(42, "somestring", nestedBrah(17));
    let y: u8 = x / 2 as u8;
    let a = x as i32;
    if (a< 3) {
        return 3;
    } else if (a > 6) {
        return 9;
    } else {
        return 4;
    }
}

fun nestedBrah(x: i32) => i32 { 
    return x * 2;
}

/*
one day we will print!
*/
fun printf(s: string) => void {
    return;
}

fun otherFunc(a: i32, s: string, b: i32) => u8 { 
    printf(s);
    return (a+b) as u8;
}

