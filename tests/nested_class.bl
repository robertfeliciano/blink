class Inner {
    let i: i32 = 0;
}

class Outer {
    let inside: Inner = new Inner { i = 44 };
}

fun main() => i32 {
    let o = new Outer { inside = new Inner { i = 55 } };
    return o.inside.i;
}